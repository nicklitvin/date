export const globals = {
    genders: ["Male", "Female"],
    minAge: 18,
    maxAge: 30,
    maxUploads: 6,
    maxAttributes: 5,
    timeBeforeChatTimestamp: 1000 * 60 * 60,
    minutesBeforeResend: 1,
    settingImages: "images",
    settingDescription: "description",
    settingGenderPreference: "gender preference",
    settingAgePreference: "age preference",
    maxPreviewMessageLength: 20,
    maxNameLength: 16,

    attributes: {
        "Sports": [
            {id: "id", value: "soccer"},
            {id: "id2", value: "basketball"},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
            {id: String(Math.random()), value: String(Math.random()).substring(0,5)},
        ],
        "Music": [
            {id: "id3", value: "pop"},
            {id: "id4", value: "rap"},
        ]
    },

    green: "#4A991A",
    red: "#B81515",
    dark: "#222222",
}